service:
  targetPort: 9090
envFrom:
  - ConfigMapRef:
      name: backend-config
  - SecretRef:
      name: backend-mysql-access
ingress:
  enabled: true
  hosts:
    - host: conference.backend
      paths:
        - "/"